# Maison de Veux — Shared Production Backend

Production backend: Supabase project `mogyngdhmzbjmcdqeoxu`.

The public roster uses the read-only `website_public_roster` RPC with the project's publishable key. The browser never receives a Supabase secret/service-role key.

Agency, Model, and Mother Agency portals remain independent Netlify applications connected to the same production Supabase project.
